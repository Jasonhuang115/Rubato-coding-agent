{
  "name": "rubato",
  "version": "0.2.0",
  "description": "Rubato — a coding agent with verified file memory, intention tracking, and auditable sessions",
  "type": "module",
  "main": "dist/cli/entry.js",
  "bin": {
    "rubato": "bin/rubato"
  },
  "files": [
    "dist",
    "bin",
    "README.md",
    ".env.example"
  ],
  "scripts": {
    "clean": "node scripts/clean-dist.mjs",
    "build": "npm run clean && tsc",
    "dev": "tsx src/cli/entry.ts",
    "migrate:legacy": "node scripts/migrate-legacy-memory.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "chalk": "^5.3.0",
    "diff": "^7.0.0",
    "openai": "^4.73.0",
    "yaml": "^2.6.0"
  },
  "optionalDependencies": {
    "better-sqlite3": "^11.6.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/diff": "^6.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
